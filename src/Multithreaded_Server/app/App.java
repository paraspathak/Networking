package app;

import app.Server;

/*
    Main Class
    Runs the server class to create the server
    Open in IDE and run this file
*/
public class App {
    public static void main(String[] args) throws Exception {
        Server server = new Server();                               //Instantiate the server
    }
}